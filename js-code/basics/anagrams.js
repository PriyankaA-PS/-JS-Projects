// Anagrams - a word formed by rearranging by the given word

// console.log(arrangeAnagram("listen", "silent"))
// output: true

function arrangeAnagram(str1, str2){

    // default of sort is ascending order
    const sortedStr1 = st1.split("").sort.join("");    //eilnst
    const sortedStr2 = str2.split("").sort.join("");   //eilnst

    return sortedStr1 === sortedStr2;

}